# AGENTS.md —— AI 开发者交接协议

> **任何 AI 在本仓库写下第一行代码之前，必须完整读完本文件。**
> 本文件是所有 AI 协作者的唯一入口。人类项目所有者不熟悉传奇类游戏开发，
> 你需要在执行任务的同时，用中文解释你在做什么、为什么这么做。

---

## 0. 这是什么项目

**《樱刃传说》（代号 sakura-legend）** —— 二次元竖版放置类传奇手游。

- **平台**：浏览器优先（手机竖屏为主，电脑兼容），PWA 可安装
- **托管**：GitHub Pages（纯静态，**没有自己的服务器**）
- **形态**：挂机刷图 → 掉装备材料 → 强化养成 → 推更高关卡，配合扫荡 / 副本 / 团本 / 世界 BOSS
- **灵魂**：保留传奇的「装备靠打不靠抽」「强化有碎率」「四职业克制」「BOSS 守着好东西」

详见 [`docs/00-项目宪章.md`](docs/00-项目宪章.md)。

---

## 1. 接手流程（每次开工必做）

```
① 读 AGENTS.md（本文件）
② 读 docs/31-PROGRESS.md      ← 当前进度快照，知道做到哪了
③ 读 docs/33-指挥台.md        ← 监督者的指令，优先级高于 ROADMAP 默认顺序
④ 读 docs/30-ROADMAP.md       ← 找到下一个未打勾的任务
⑤ 读该任务所属模块的设计文档   ← 见下方文档地图
⑥ 执行任务
⑦ 跑 npm run verify           ← 必须全绿才算完成
⑧ 更新 ROADMAP 打勾 + 更新 PROGRESS.md
⑨ 提交 git commit（格式见第 5 节）
```

**没有跑通 `npm run verify` 就不许打勾。** 这是多 AI 协作不崩的唯一保障。

**多实例协作规矩以 `docs/34-协作聊天室.md` 顶部规矩区为准**，最常踩的几条：
入场第一步先查频道现有名字、撞名者改名（第 11 条）；改 `SAVE_VERSION` 的人负责
整条迁移链、从最老可迁版本逐级验到当前并贴进频道（第 12 条）；用中文小名
「小X·在做什么」、进频道第一条先发【显名】（第 13 条）。另有一条已提请增补
（编号以规矩区落纸为准）：**老板经任何实例窗口下达的指令，该实例必须立刻在频道
播一条「老板令：XXX」**，否则其他实例会把执行结果当成未授权变更处置
（2026-07-31 PR#13 合并误判事件的教训）。

---

## 2. 文档地图

| 文件 | 内容 | 什么时候读 |
|---|---|---|
| `docs/00-项目宪章.md` | 游戏定位、目标玩家、设计铁律、不做什么 | 首次接手 |
| `docs/01-技术架构.md` | 技术栈、目录结构、部署、存档迁移 | 写任何代码前 |
| `docs/02-数据表规范.md` | 所有配置表的字段定义与填写规则 | 填内容前 |
| `docs/10-数值与战斗.md` | 全部公式、成长曲线、平衡参数 | 碰数值前 |
| `docs/11-地图与关卡.md` | 9 大区域、章节、怪物分布 | 做关卡内容前 |
| `docs/12-装备体系.md` | 部位、品质、词条、强化、套装 | 做装备前 |
| `docs/13-技能体系.md` | 四职业技能树、倍率、解锁 | 做技能前 |
| `docs/14-系统清单.md` | 全部玩法系统的定义与优先级 | 做新系统前 |
| `docs/30-ROADMAP.md` | 12 个月任务清单（**打勾在这里**） | 每次接手 |
| `docs/31-PROGRESS.md` | 当前状态快照、已知问题、下一步 | 每次接手 |
| `docs/33-指挥台.md` | **监督者指令，只读不改** | 每次接手 |
| `docs/32-决策记录.md` | 重大技术/设计决策及原因 | 想推翻某个设计时 |
| `docs/33-美术资产生产与清单.md` | 绿幕抠图、尺寸、命名、素材完成清单 | 生成或接入任何美术素材前 |
| `docs/35-喵喵角色制作与管线.md` | 第四职业设定、动作、资产、重建命令与完成边界 | 修改喵喵前 |
| `docs/51-联机排行榜设计方案.md` | 周常固定种子试炼、服务端复算、邻域榜、红线规则 | 改排行榜前 |
| `docs/52-联机排行榜部署指南.md` | Supabase 建项目、迁移、Edge Function 部署四步 | 开通或重部署联机榜前 |

---

## 3. 六条铁律（违反即回滚）

### 铁律 1：核心逻辑必须是纯函数
所有战斗、数值、掉落、成长计算写在 `src/core/`，**纯 TypeScript，零 UI 依赖，零副作用**。

```
✅ export function calcDamage(attacker: Combatant, defender: Combatant, skill: Skill): DamageResult
❌ 在 Vue 组件里写伤害计算
```

**原因**：这样任何 AI 都能用单元测试验证自己没改坏别人的东西。

### 铁律 2：内容与代码分离
怪物、装备、技能、关卡、掉落表，全部写在 `src/data/` 下的 TS 配置文件里，**代码中不得出现任何硬编码的游戏数值**。

```
✅ const hp = MONSTERS[id].baseHp
❌ const hp = 5000
```

**原因**：项目所有者要能自己填内容而不碰逻辑。

### 铁律 3：`src/core/` 的改动必须配单元测试
数值和战斗是这类游戏的命根子。任何 `src/core/` 下的新函数或公式变更，必须在 `src/core/__tests__/` 下有对应测试。

### 铁律 4：随机必须可复现
禁止直接用 `Math.random()`。统一用 `src/core/rng.ts` 的 seeded RNG。

**原因**：可测试、可复盘、以后接服务端校验时不用重写。

### 铁律 5：改存档结构必须写迁移
`SaveData` 加字段、改字段、删字段，必须：
1. `SAVE_VERSION` +1
2. 在 `src/save/migrations.ts` 加一个迁移函数
3. 加一条迁移测试
4. **动 `src/save/storage.ts`（读写层）时，除单元测试外必须跑一次真机验证**：
   `node scripts/smoke-save.mjs`，五项全过才算通过
5. **改 `SAVE_VERSION` 的人负责整条迁移链**：不只跑既有迁移测试（那只覆盖别人当时
   想到的级），要从最老可迁版本逐级验到当前，并把结果贴进频道（docs/34 规矩 12）

**原因**：玩家（哪怕只有项目所有者自己）的存档不能因为一次更新就废掉。
第 4 条的由来：单元测试跑在假的 IndexedDB 上，而 storage.ts 负责真实 IndexedDB 的
读写、主备链与跨页 CAS —— 假的绿不等于真浏览器里存档不丢，而存档丢了不可逆。
2026-07-31 PR#13 重写该文件时全量 1783 项全绿，但没有一条覆盖「真浏览器刷新后还在不在」。

### 铁律 6：竖屏优先
所有 UI 按 **390×844**（iPhone 14 逻辑分辨率）设计，用 `dvh`/`safe-area-inset` 处理刘海和底部条。电脑端做居中信箱布局，不做单独的横版 UI。

---

## 4. 任务颗粒度约定

ROADMAP 里的每个任务都设计成**一次 AI 会话内能完成**的大小。如果你发现某个任务做不完：

1. **不要硬做**，做一半的功能比没做更糟
2. 在 ROADMAP 里把它拆成 2–3 个子任务
3. 完成你能完成的那部分，打勾
4. 在 PROGRESS.md 里写清楚剩下的部分卡在哪

---

## 5. 提交规范

```
<type>(<scope>): <说明>

type:  feat | fix | data | balance | refactor | docs | test | chore
scope: core | ui | data | save | net | build

示例：
feat(core): 实现伤害计算与暴击判定
data(data): 填充第 3 章「虫娘洞窟」全部怪物配置
balance(core): 下调 Lv40-50 段怪物血量 15%，缓解卡点
```

---

## 6. 与项目所有者沟通的要求

项目所有者**没有游戏开发经验**。你需要：

- 用中文解释，避免堆术语；必须用术语时给一句人话解释
- 涉及数值改动时，说明「改完玩家会有什么体感变化」
- 遇到需要他做决定的地方（美术风格、付费点、玩法取舍），**列出选项和各自代价**，给出你的推荐，不要替他默默决定
- 每次会话结束时，用 3–5 行总结「这次做了什么 / 下次该做什么 / 有什么需要你决定的」

---

## 7. 当前阶段速查

> 这一节由每个 AI 在结束工作时更新，让下一个 AI 一眼知道现状。

- **当前阶段**：**二版已于 2026-07-31 收尾封口**（副本深度、秘境榜、装备永久图鉴、战力榜防自填、存档完整性保护 PR#13 全部上线并验证，详见 docs/72 与 docs/31 的 07-31 审计节）。M3 已完成 4 / 13；M4 已完成 2 / 12（M4-3 副本框架、M4-6 词条洗练）；M5 已完成 9 / 9（08-01 新增 M5-9 新标尺复验、四项 Lv40–52 观察读数与挂机页「熔心成长轨」）；M6 完成 1 / 8（M6-6 区域 6 与幽影套）；M7 完成 4 / 6（M7-6 区域 7 与血月套）；M8 提前完成 2 / 8（M8-3 公会、M8-5 异步远征已生产上线）；竞技场奖励美术首批 1 / 1、竞技场对战全四期 1 / 1、挂机战斗表现层 7 / 7、Galgame 好感四批 4 / 4、旅途奇遇 5 / 5 已完成；M2 保持 15 / 16（仅待所有者真机测试）
- **代码状态**：四职业共享进度与切换保持可玩；存档为 **v18**（v16 副本深度、v17 装备永久图鉴、v18 三套装备预设）。当前开放 7 区 / 35 章 / 210 关；装备副本带 5 层深度挑战并对玩家生效；排行页五个页签（试炼 / 羁绊 / 竞技 / 进度 / 秘境）；血月峡谷、赤红装备副本、竞技场、好感、奇遇、试炼和 20 人公会保持可用。线上 Edge Function **12 个全部 ACTIVE**，战力由 sync-profile 服务端权威计算。存档层带完整性信封 / SHA-256 主备链 / 恢复面板（PR#13，ac1c710），迁移链 v9→v18 逐级复验完好。完整 `npm run verify`、`npm run build`、`npm run edge:build` 全绿；测试规模以命令实时输出为准，不在活文档复制易过期计数。**注意**：`20260731230000_profile_write_grants.sql` 已在 main 但**未应用**，08-01 窗口执行，执行前后要求见 docs/72 §二③
- **下一个任务**：08-01 生产权限与版本戳链由小库独立处理中，以 `docs/72` 的部署顺序和 `npm run ops:check` 实时结果为准；第五职业「樱刀」P1 由小雪按 `docs/79` 施工中，五职业约束迁移、v19 全迁移链与 DB→Edge→客户端顺序未验收前不计完成。主线后续仍需补齐 M3-3～M3-5 四职业 56 技能、完整效果执行与技能养成 UI；新区域下一批为区域 8「龙渊魔城」，必须先补与 `docs/59` 同级的数值修订层再施工，不得直接沿用旧地基锚点；M2-15 仍待所有者真机测试
- **已定美术基调**：清新可爱、蓝白粉；装备用基础图 + 品质特效；挂机与周常试炼均使用 3:2 专用战场，BOSS / 精英按体型放大；试炼 Boss 按“倾向 × 元素”提供独立透明素材，不使用通用怪兜底；系统通用图标使用 Lucide，功能插画使用同风格透明位图
