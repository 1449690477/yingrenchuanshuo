# Supabase 联机接入（排行榜，见 docs/51-联机排行榜设计方案.md）
#
# 用法：复制本文件为 .env.local 并填入真实值。
# .env.local 已在 .gitignore 里，不会被提交。
#
# ── 这两个值可以公开 ──
# 它们会被打进网页包，任何玩家都能看到 —— 这是 Supabase 的设计，
# 安全性由数据库的 RLS 策略保证，不是靠藏 key。
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...

# ── 绝对不要放这里 ──
# service_role key 拥有绕过 RLS 的完全权限。
# 它只属于服务端（Supabase Edge Function），由 Supabase 平台自己注入，
# 永远不要写进本项目任何文件、不要贴进聊天室、不要设成 VITE_ 开头的变量
# （VITE_ 前缀的变量一律会被打进客户端包）。
