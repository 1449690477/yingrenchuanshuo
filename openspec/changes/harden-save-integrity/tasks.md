## 1. 本地完整性基元

- [x] 1.1 实现稳定序列化、SHA-256 摘要和版本化存档信封纯函数
- [x] 1.2 覆盖对象顺序、特殊数字拒绝、摘要篡改和父链断裂测试

## 2. IndexedDB 兼容接入

- [x] 2.1 主槽、备份槽和 revision 原子写入完整性信封
- [x] 2.2 兼容旧裸快照首次升级，不修改 `SAVE_VERSION`
- [x] 2.3 完整性失败停止自动写入并保留原始槽位
- [x] 2.4 覆盖多标签 CAS、备份恢复、清档墓碑和写入失败反例

## 3. 导入导出与交互

- [x] 3.1 JSON 导入建立 imported 来源链且拒绝可信元数据注入
- [x] 3.2 存档设置页展示完整性状态、备份恢复和原始数据导出
- [x] 3.3 在 390×844 / 320×568 下验收长错误信息、44px 按钮和焦点恢复

## 4. 性能与完整验证

- [x] 4.1 测量 300 件装备存档摘要与保存耗时，设置 250ms 硬门禁
- [x] 4.2 运行 OpenSpec 严格校验、`npm run verify` 和 `npm run build`
- [x] 4.3 更新存档架构、部署边界、ROADMAP 和 PROGRESS

## 后续独立变更

服务端权威资产账本不属于本 change。装备 UID、货币、奖励、离线兑换和交易必须在独立 OpenSpec 中设计、实现并完成双账号生产验收；在此之前不得开放资产型公会奖励或交易。
