# 68 · 装备副本深度阶梯 UI（docs/66 §八 第 6 步 · ①选深度 + ③失败不扣次数）

> 状态：**已就位、挂激活开关（false），对玩家零可见变化**。
> 翻 true 的时机：claude-drops 的掉落表接线批次（docs/66 §八 第 5 步）落地时，
> 由接线方翻开关、删 stub、直连 game store —— 与烙印激活批次同一条先例。

## 一、本批做了什么

22:04 频道派给 kimi 侧的三件 UI，本批完成其中两件：

| # | 事项 | 状态 |
|---|---|---|
| ① | DungeonView 第二步加「深度阶梯」：4 档 × 5 层选择面板 | ✅ 本批 |
| ② | 奇迹胚子显影（掉落卡演出 + 留痕） | ⏸ 暂缓，见 §四 |
| ③ | 深度进度写明「失败不扣次数」（§4.4 承重） | ✅ 本批 |

交付物：

- `src/components/dungeon/DungeonDepthPanel.vue` —— 纯展示组件，五种节点状态：
  - **已突破**：显示稳定后胚子掉率（12/18/26/36/50%）—— 重刷这层的动机
  - **下一层可打**：「首破必掉 1 件胚子」徽标 + 推荐战力，脉冲辉光 + 粒子焦点
  - **锁链**（previous-depth）：「先突破上一层」—— 深度链替代旧等级门槛
  - **区域未开放**（not-opened，crimson d2~d5）：「区域 8 开放后解锁」，**不显示任何门槛数字**（K5 同款红线）
  - **次数尽**（daily-limit）：「今日奖励已领完」
  - 页脚承重句：「失败不扣次数、不推保底 —— 冲深度是免费的；每日 3 次限的是奖励，不是尝试。」
- `src/ui/dungeonDepthActivation.ts` —— 激活开关，默认 false
- `src/ui/dungeonDepthAdapter.ts` —— 本地数据源 stub（接线批次整体删除）
- `src/components/__tests__/dungeonDepthUi.spec.ts` —— 18 条：源码契约 + stub 单测 + jsdom 挂载冒烟
- `src/views/DungeonView.vue` —— 集成：面板挂在 `v-if="depthUiActive"` 后面；
  开关开时挑战按钮禁用并提示「深度挑战随接线批次开放」、peek 显示选中深度

## 二、stub 的三条假设（claude-drops 已在频道逐条核过）

与 docs/66 §五 迁移规则同口径，保证**接线前后玩家看到的 UI 不跳变**：

1. **progress 推导**：该档任一部位有首通记录 ⇒ 视为 d1 已过，否则 0；
   绝不伪造更高深度（哪怕 32 座全首通，每档也只记 1）
2. **evaluate 直连** `core/equipmentDungeonDepth.ts`（cf828ab 已落地），
   `contentTopLevel = max(ALL_CHAPTERS.levelTo)`，与 arenaEquipment 同源
3. **run 不开放**：stub 期挑战按钮禁用，文案「深度挑战随接线批次开放」

reason 优先级（core 实现，UI 依赖此序写文案）：
**not-opened > previous-depth > daily-limit > ok**。
注意 `attemptsRemaining` 传 0 会让所有可打层返回 daily-limit（面板一片灰）——
stub 期直连现成的 `game.equipmentDungeonRemaining`，不要传错。

## 三、红线与守卫（都有测试钉着）

- **开关锁 false**：契约测试同时断言源码字面量与运行时值，
  翻 true 必须连带删 stub，防止「开关开着、stub 还在」的半成品
- **not-opened 不显示门槛数字**：测试断言该分支源码不含 Lv/unlockLevel/recommend
- **只做正向峰**：测试断言面板与视图源码不含任何负向奇迹措辞
  （docs/66 §4.3 / G-9 / docs/40 损失厌恶）。⚠ 注释里也别写原话——
  守卫测试是全文扫描，注释里的负向例句会把自己咬住
- **减弱动效**：粒子在模板层整个不渲染（不是只停动画），与羁绊榜同一条先例

## 四、②奇迹胚子显影为什么暂缓

显影要渲染「胚子掉落卡」，而掉落形状此刻正被烙印激活批次改
（整装 → 材料，`equipmentDungeons.ts` / `equipmentDungeonGear.ts` 在飞）。
**在移动的地上不打地基。**

接手的时机与红线（已记入待办）：

- 时机：claude-drops 第 5 步接线冻结胚子结果形状当天，他会在频道点名
- 范围：掉落卡演出按胚子档（稳定/精工/奇迹）升级 + 图鉴/记录留痕
- 红线：**只做「出了奇迹」的正向峰，绝不做「这次没出」的负向提示**
  （docs/40 禁止焦虑与损失厌恶；G-9 进验收表）
- 不加任何数值，只把已经存在的方差显影

## 五、接线批次交接清单（给 claude-drops）

翻开关那批要做的事，按序：

1. `game store` 接上三签名：`equipmentDungeonDepth` / `evaluateDungeonDepth(tierId, depth)` / `runEquipmentDungeonDepth(slot, tierId, depth)`
2. DungeonView 改直连 game store，**删除** `src/ui/dungeonDepthAdapter.ts`
3. `challenge()` 改走 `runEquipmentDungeonDepth(selectedSlot, selectedTierId, selectedDepth)`；
   删掉 lockCopy/challengeDisabled 里的 stub 分支
4. `src/ui/dungeonDepthActivation.ts` 翻 true，并改掉契约测试里的 false 断言
5. 存档迁移按 §五 走 SAVE_VERSION+1（stub 的 progress 推导与迁移规则已对齐，UI 不会跳变）

## 六、验收记录

- `dungeonDepthUi.spec.ts`：18/18 绿（源码契约 5 + stub 单测 8 + 挂载冒烟 5）
- 全量 vitest / vue-tsc / build / eslint：在**隔离 worktree**（干净 origin/main + 本批文件）实跑，
  结果见提交信息。主树有 claude 的在飞改动，不在主树跑全量（docs/34 协作规则）
- 适配：320px 档有媒体查询（五层仍一屏可读）；粒子/脉冲/流光全部响应 reduceMotion
