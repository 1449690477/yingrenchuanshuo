# 33 · 指挥台（监督者 → 开发 AI）

> **这是项目监督者下达指令的地方。开发 AI 只读，不要修改本文件。**
>
> 每次开工时，在读完 `31-PROGRESS.md` 之后读这里。
> 本文件的指令**优先级高于 ROADMAP 的默认顺序**。
> 完成后不要在这里打勾 —— 在 `30-ROADMAP.md` 和 `31-PROGRESS.md` 里记录即可。

---

## 当前指令（2026-07-27 更新）

### 1. 先清理仓库卫生（优先级最高，10 分钟内做完）

- [x] ~~删除根目录临时文件~~ → **监督者已代为处理**：`.gitignore` 已加入 `tmp-*.png` / `tmp-*.jpg` 规则。
      现在根目录已堆了 **10 个** `tmp-*.png`（从 1 个涨到 10 个）。
      **请在当前美术任务结束后统一删除它们**，并改用 `scripts/out/` 或系统临时目录做中间产物，
      不要再往仓库根目录写临时文件。
- [ ] 决定 `art-source/` 的归属：
  - 若是**可再生的中间产物**（合成脚本能重新生成）→ 加入 `.gitignore`
  - 若是**不可再生的源文件** → 提交入库，并在 `docs/01-技术架构.md` 补一节说明美术资源目录约定
- [ ] `public/assets/characters/modular/` 属于运行时资源，**必须提交**
- [ ] 检查是否有单个文件超过 2MB，有的话说明原因或压缩

**为什么现在做**：未跟踪文件堆积会让后面的 AI 分不清哪些是有效资源，
而且大文件一旦进历史就很难移除。

### 2. 完成 M2，然后进 M3

- [ ] 补完 **M2-15**（真机测试），M2 收尾
- [ ] 进入 **M3**，严格按 M3-1 → M3-13 顺序

### 3. M3 阶段的重点要求

**这一阶段的重心是玩法深度，不是美术。** 美术已经够用了（立绘、地图、图标都已实装），
M3 期间除非某个新系统缺图标，否则不要再新增美术工作。

优先级排序：

1. **M3-1 强化系统**（`core/enhance.ts`）—— 这是传奇的灵魂，必须先做
   - 成功率、掉级、碎裂、**幸运值保底**（见 ADR-004，保底不可省）
   - 必须配单元测试，覆盖「连续失败 N 次必成」
2. **M3-3 / M3-4 技能系统** —— 三职业 42 个技能 + 自动释放逻辑
3. **M3-6 / M3-7 体力与扫荡**
4. 其余按 ROADMAP 顺序

### 4. 数值红线（不可违反）

做完 M3-1（强化）和 M3-4（技能）之后，**必须跑 `npm run sim` 并检查**：

| 指标 | 目标 |
|---|---|
| 击杀单只小怪耗时 TTK | 5 ± 1.5 秒（全等级段） |
| 三职业挂机效率偏离 | ±20% 以内 |
| 每秒击杀 | 不得撞上 `maxKillsPerSec` 上限 |
| 30 天成长 | 理想玩家约 15 天满级，不得出现等级停滞 |

强化（最高 ×2.2）和技能倍率会显著抬高玩家 DPS。如果曲线被打破，
**调 `src/data/constants.ts` 的曲线参数，不要调单个装备或怪物**，
并在 `32-决策记录.md` 记一条 ADR。

背景：M1-10 阶段已经因为指数关系没对齐翻过一次车（ADR-005/006/007），
教训是「单个指数没有意义，只有指数之间的差值有意义」。

---

## 追加指令（2026-07-27 10:40）

监督者确认：临时文件已清理、`art-source/` 归属已在 `docs/01-技术架构.md` 写明。做得好。

现在有两个问题需要立刻处理：

### A. 提交太少，未提交改动堆积

最近一次提交是 `ab8f0fe`，距今已 **1 小时**，工作区累积 **31 个变更文件 / 633 行**。

**要求**：按 AGENTS.md 第 1 节的流程，**每完成一个 ROADMAP 任务就提交一次**。
一小时不提交，一旦出错要回滚就得丢掉一整段工作。
现在请先把手头这批改动按逻辑拆成 2–3 个提交推上去。

### B. M3 的玩法代码还没开始

`src/core/enhance.ts` 和 `src/core/skills.ts` **都还不存在** ——
说明 M3-1（强化）和 M3-4（技能自动释放）都没动。

**M3 是玩法里程碑，不是美术里程碑。** 美术已经足够支撑到 M5 了。
请在提交完手头改动后，**立刻转向 M3-1**，顺序不要跳：

1. `core/enhance.ts` —— 成功率表、失败掉级、+13 起碎裂、**幸运值保底**（ADR-004）
2. 配套单元测试，必须覆盖「+15 连续失败 8 次后必定成功」这条保底
3. 然后才是强化 UI（M3-2）

判断标准：M3-1 完成时，`src/core/__tests__/enhance.spec.ts` 应该存在且全绿。

---

## 已完成的指令（存档）

（暂无）
