# 31 · 进度快照

> **每个 AI 结束工作时必须更新本文件。**
> 下一个接手的 AI 只看这一页就应该知道现在什么状况。

---

## 当前状态

| 项 | 值 |
|---|---|
| **更新时间** | 2026-07-26 |
| **当前里程碑** | **M0 + M1 已完成** → 下一步 M2 最小可玩闭环 |
| **完成度** | 17 / 178 |
| **代码状态** | `npm run verify` 全绿（88 个测试），`npm run build` 通过，`npm run sim` 曲线健康 |
| **可玩状态** | 不可玩（核心逻辑已就绪，但还没接 UI 和存档） |
| **GitHub 仓库** | https://github.com/1449690477/yingrenchuanshuo |
| **部署地址** | https://1449690477.github.io/yingrenchuanshuo/ |

## M1 阶段的重要发现（必读）

M1-10 的数值校验**推翻了文档里三处原始设计**，全部记录在 `32-决策记录.md`：

| ADR | 问题 | 结论 |
|---|---|---|
| **ADR-005** | 原 `monsterHp = 200 × L^2.3` 导致 Lv50 杀一只怪要 **3800 秒**，游戏中期直接卡死 | 怪物曲线指数必须贴近装备的 `ITEM_POW(1.35)`，改为 `60 × L^1.45` |
| **ADR-006** | 暴击率被和攻击力一样乘装备基准值，Lv120 单件武器给 **115% 暴击率** | 百分比属性拆到 `SLOT_PCT_WEIGHTS`，只随品质缩放，不随等级 |
| **ADR-007** | 装备是职业通用的，把职业属性差异冲淡到失效，攻速最高的灵巫反而输出最高 | 新增 `CLASS_ATK_MUL` 百分比系数，作用于装备累加之后 |

**教训（写给后面每一个 AI）**：
数值设计里**单个指数没有意义，只有指数之间的差值有意义**。
想改 `MONSTER_HP_POW`，必须同时看 `ITEM_POW`。改完必须跑 `npm run sim`。

## 当前数值健康状况

| 指标 | 实测 | 目标 | |
|---|---|---|---|
| 击杀单只小怪耗时（TTK） | 4.8 – 6.7 秒 | 5 ± 1.5 秒 | ✅ |
| 30 天成长（理想玩家） | Day1 Lv25 → Day15 满级 120 | 真实玩家约 30 天 | ✅ |
| 三职业效率偏离 | ±17%（Lv10 最差），高等级收敛到 ±9% | ±20% 内 | ✅ |
| 每秒击杀是否撞上限 | 否（Lv120 为 1.11，上限 3.0） | 不应撞上限 | ✅ |

## 下一个任务

**M2-1** `save/schema.ts` + `save/storage.ts`：IndexedDB 存档读写，自动存档

M2 是第一个「能玩」的里程碑。做完这 16 个任务后，手机上打开应该能：
选职业 → 挂机打怪 → 掉装备 → 穿上 → 变强 → 关掉再打开有离线收益。

**建议顺序**：先做 M2-1~M2-4（存档 + store 骨架），再做 M2-5~M2-11（UI），
最后 M2-13~M2-16（内容填充 + 真机测试）。**不要先做 UI 再补存档**。

## 环境状态

| 项 | 状态 |
|---|---|
| Node.js | ✅ v24.18.0 |
| npm | ✅ 11.16.0 |
| Git | ✅ 2.54.0，已配好 GCM 凭据助手 |
| GitHub 仓库 | ✅ 已创建并推送 |
| GitHub Pages | ✅ Source 已设为 GitHub Actions |
| Supabase | ⬜ 未注册（M7 才需要） |

## 已知问题 / 技术债

| # | 问题 | 处理时机 |
|---|---|---|
| 1 | PWA 图标是脚本生成的渐变占位图（`scripts/gen-icons.mjs`） | M12-3 |
| 2 | `TopBar.vue` 数据为硬编码占位 | M2-11 |
| 3 | 5 个 Tab 页面是占位 `StubPanel` | M2 各任务 |
| 4 | **模拟器的装备模型是「理想满配」假设**（`scripts/simulate.ts` 的 `typicalQuality`），不是真实装备计算 | M5 套装落地后换成真实计算 |
| 5 | 模拟器只算单体 DPS，魔女的 AoE 优势未纳入 | M3-4 技能逻辑做完后补 |
| 6 | 未使用 vue-router，Tab 切换是 Pinia 状态 | 需要浏览器返回键时再改 hash 路由 |
| 7 | `monsterGold` 复用了 `MONSTER_TYPE_MUL.exp` 系数（金币和经验共用类型倍率） | 若要让 BOSS 金币独立调整，需拆出 gold 系数 |
| 8 | `docs/12` 的词条池数值范围尚未在代码中实现，M4-6 洗练系统时才会用到 | M4-6 |

## 待项目所有者决定

| 问题 | 选项 | 影响 |
|---|---|---|
| **美术风格基调** | 明亮日系 / 暗黑哥特 / Q 版 SD | 影响全部 AI 立绘 prompt。当前 UI 是「暗紫底 + 樱粉」，偏日系轻暗 |
| 是否做内购 | 纯免费 / 预留接口 | 影响数值松紧，M11 前要定 |

## 变更日志

| 日期 | AI | 做了什么 |
|---|---|---|
| 2026-07-26 | Claude | 建立完整文档体系（宪章、架构、数值、地图、装备、技能、系统、路线图、决策记录） |
| 2026-07-26 | Claude | **M0 完成**：工程骨架、校验链、分层 lint 规则、CI/CD、PWA、竖屏布局、移动端适配 |
| 2026-07-26 | Claude | **M1 完成**：类型、seeded RNG、全部战斗公式、成长曲线、掉落（含保底）、挂机与离线结算、88 个单元测试、数值模拟器；模拟器校验推翻并修正了 3 处原始数值设计（ADR-005/006/007） |
